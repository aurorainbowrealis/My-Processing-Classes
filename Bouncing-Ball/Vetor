public class Vetor{
  public double x, y;
  public Vetor(){
    this.x = 0;
    this.y = 0;
  }
  
  public Vetor(double x, double y){
    this.x = x;
    this.y = y;
  }
  
  public Vetor Soma(Vetor b){
    return new Vetor(x + b.x, y + b.y);
  }
}
